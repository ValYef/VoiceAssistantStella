<Window 
    x:Class="VoiceAssistant.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VoiceAssistant"
    xmlns:classes="using:VoiceAssistant.Components.Sphere"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Voice Assistant Stella"
    >

    <Grid Background="#FF1E1E1E">
        <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- Шапка -->
            <RowDefinition Height="40"/>
            <!-- Вивід команди -->
            <RowDefinition Height="*"/>
            <!-- Сфера і кнопки -->
        </Grid.RowDefinitions>

        <!-- Шапка -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" >
            <TextBlock x:Name="volumeTextBlock"
                       Text="Гучність: 0.00"
                       FontSize="12"
                       Foreground="White"
                       Margin="0,0,10,0"/>
        </StackPanel>

        

        <!-- Основна частина -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                <!-- Сфера -->
                <ColumnDefinition Width="*"/>
                <!-- Текст та кнопки -->
            </Grid.ColumnDefinitions>

            <!-- Анімована сфера -->
            <classes:SphereControl x:Name="animatedSphere"
                                   Margin="0,0,5,0"
                                   Grid.Column="0"
                                   Width="500"
                                   Height="500"/>

            <!-- Текст результату та кнопка -->
            <Grid Grid.Column="1"
                  MinWidth="600"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Center"
                  >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                    <Button x:Name="myButton"
                        Grid.Row="0"
                        Click="myButton_Click"
                        HorizontalAlignment="Stretch"
                        Content="Почати слухати"
                    />

                    <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="myTextBlock"
                               Text="Результат розпізнавання з’явиться тут..."
                               TextWrapping="Wrap"
                               Foreground="White"
                               VerticalAlignment="Top"/>
                </ScrollViewer>
            </Grid>
        </Grid>
        </Grid>

        <!-- Вивід команд -->
        <TextBlock Grid.Row="1"
           x:Name="commandOutputTextBlock"
           Text="Тут з'являтимуться команди"
           FontSize="18"
           Foreground="White"
           HorizontalAlignment="Center"
           VerticalAlignment="Top"
           TextWrapping="Wrap"
           Padding="10"
           Margin="0,50,0,5"/>
    </Grid>
</Window>
